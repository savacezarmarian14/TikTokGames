#include "render/HudRenderer.hpp"

namespace tw {

void HudRenderer::render(sf::RenderTarget& target,
                         const GameState& state,
                         const sf::Font& font,
                         const sf::Vector2u& windowSize) const {
    (void)target;
    (void)state;
    (void)font;
    (void)windowSize;
}

} // namespace tw
